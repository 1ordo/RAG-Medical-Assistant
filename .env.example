# Medical Assistant AI Configuration
# Copy this file to .env and configure your preferred AI provider

# AI Provider Selection ('ollama' or 'gemini')
# If not set, the app will auto-detect based on availability
AI_PROVIDER=ollama

# Ollama Configuration (for local AI)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# Google Gemini Configuration (for cloud AI)
# Get your API key from: https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# Note: The app will automatically use Gemini if Ollama is not available
# and a Gemini API key is configured, or vice versa.
